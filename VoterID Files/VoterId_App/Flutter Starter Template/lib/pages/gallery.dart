import 'package:flutter/material.dart';

class galleryPage extends StatelessWidget {
  const galleryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('galleryPage'),
      ),
      body: const Center(
        child: Text('Work In progress'),
      ),
    );
  }
}
