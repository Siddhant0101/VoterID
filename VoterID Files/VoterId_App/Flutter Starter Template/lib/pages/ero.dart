import 'package:flutter/material.dart';

class eroPage extends StatelessWidget {
  const eroPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('eroPage'),
      ),
      body: const Center(
        child: Text('Work In progress'),
      ),
    );
  }
}
