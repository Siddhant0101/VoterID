import 'package:flutter/material.dart';

class bloPage extends StatelessWidget {
  const bloPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('bloPage'),
      ),
      body: const Center(
        child: Text('Work In progress'),
      ),
    );
  }
}
