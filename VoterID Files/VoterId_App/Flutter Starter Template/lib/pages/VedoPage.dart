import 'package:flutter/material.dart';

class VedoPage extends StatelessWidget {
  const VedoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('VedoPage'),
      ),
      body: const Center(
        child: Text('Work In progress'),
      ),
    );
  }
}
