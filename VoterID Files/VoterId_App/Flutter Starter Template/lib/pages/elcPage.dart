import 'package:flutter/material.dart';

class elcPage extends StatelessWidget {
  const elcPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('learningManualPage'),
      ),
      body: const Center(
        child: Text('Work In progress'),
      ),
    );
  }
}
