import 'package:flutter/material.dart';

class sveepPage extends StatelessWidget {
  const sveepPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('sveepPage'),
      ),
      body: const Center(
        child: Text('Work In progress'),
      ),
    );
  }
}
