import 'package:flutter/material.dart';

class pwdrPage extends StatelessWidget {
  const pwdrPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('resultPage'),
      ),
      body: const Center(
        child: Text('Work In progress'),
      ),
    );
  }
}
